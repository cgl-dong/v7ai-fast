┌─────────────────────────────────────────────────────────────────────┐
│                    Day4 Plan-Solve Agent 全流程                       │
└─────────────────────────────────────────────────────────────────────┘

  用户输入: "先计算(100+25)*4，再查询什么是RAG，最后汇总"
       │
       ▼
╔══════════════════════════════════════════════════════════════════╗
║  Phase 1: PLAN（规划阶段）                                        ║
║  ┌─────────────────────────────────────────────────────────────┐ ║
║  │  LLM (temperature=0) 一次性拆解为结构化子任务列表              │ ║
║  │  Pydantic Schema 强约束 + JSON 正则兜底 + 失败自动重试        │ ║
║  └─────────────────────────────────────────────────────────────┘ ║
║                              │                                    ║
║                              ▼                                    ║
║  TaskPlan { task_list: [                                          ║
║    SubTask(t1, calc,    "125+36",         rely=[])                 ║
║    SubTask(t2, calc,    "上一步结果*8",    rely=[t1])              ║
║    SubTask(t3, rag,     "RAG定义",        rely=[])                 ║
║    SubTask(t4, summary, "汇总计算+检索",   rely=[t2,t3])           ║
║  ] }                                                              ║
╚══════════════════════════════════════════════════════════════════╝
       │
       ▼
╔══════════════════════════════════════════════════════════════════╗
║  Phase 2: EXECUTE（调度执行阶段）                                  ║
║                                                                    ║
║  TaskScheduler 调度器:                                             ║
║  ┌──────────────────────────────────────────────────────────────┐ ║
║  │  Round 1: 并行执行 (t1, t3)  ← 无依赖                         │ ║
║  │    ├─ t1 [calc] ──▶ calculator(125, 36, +) ──▶ "125+36=161"  │ ║
║  │    └─ t3 [rag]  ──▶ Indexer.search("RAG定义") ──▶ "RAG是..." │ ║
║  │                                                                 │ ║
║  │  Round 2: 串行执行 (t2)  ← 依赖 t1 完成                        │ ║
║  │    └─ t2 [calc] ──▶ calculator(161, 8, *) ──▶ "161*8=1288"   │ ║
║  │                                                                 │ ║
║  │  Round 3: 串行执行 (t4)  ← 依赖 t2, t3 都完成                  │ ║
║  │    └─ t4 [summary] ──▶ LLM 汇总推理                            │ ║
║  └──────────────────────────────────────────────────────────────┘ ║
║                                                                    ║
║  状态管理: pending → running → success/failed                      ║
║  失败重试: 单任务最多重试 2 次，仍失败则标记并继续                   ║
╚══════════════════════════════════════════════════════════════════╝
       │
       ▼
╔══════════════════════════════════════════════════════════════════╗
║  Phase 3: SUMMARIZE（汇总阶段）                                    ║
║  ┌──────────────────────────────────────────────────────────────┐ ║
║  │  LLM 整合所有子任务结果 → 生成最终完整回答                      │ ║
║  │  输入: 原始问题 + 各子任务执行结果汇总                          │ ║
║  │  输出: 结构化的最终答案（含计算过程、知识引用、失败说明）         │ ║
║  └──────────────────────────────────────────────────────────────┘ ║
╚══════════════════════════════════════════════════════════════════╝
       │
       ▼
  返回前端:
  {
    user_query,      // 原始问题
    task_list,       // 拆解的子任务列表
    task_detail,     // 每个子任务的状态+结果
    task_raw_summary,// 汇总文本
    final_answer,    // 最终 Markdown 回答
    elapsed          // 总耗时
  }